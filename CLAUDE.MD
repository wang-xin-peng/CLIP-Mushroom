# CLAUDE.md – 真菌细粒度识别与多模态检索系统

## 项目目标
使用深度学习技术，完成一个基于CLIP的真菌细粒度识别与多模态检索系统，用于课程大作业提交与答辩。
本项目的核心方向为**多模态+细粒度识别**，整体定位为一个**完整、实用、可演示的计算机视觉系统**。

## 使用场景
- 用户拍摄或上传一张真菌（蘑菇）照片，系统自动预测其科学名称（物种）。
- 用户可以用自然语言描述（如“红色伞状有毒蘑菇”），从图像库中检索出匹配的真菌图像。
- 系统可同时支持零样本识别和经过微调的识别模式，展示从通用CLIP到专用真菌数据的迁移过程。

## 数据集
- **名称**：FungiTastic-Mini（Kaggle: picekl/fungitastic）
- **内容**：包含约35万条野外观察记录，涵盖超5000个真菌物种，带有图像与元数据（时间、地点、气象等）
- **使用方式**：
  - 仅使用图像与物种标签（scientific_name）作为主要数据
  - 自动划分train / val / test，比例70:15:15，按观察记录ID分，避免同一物种的不同照片泄露到不同集合（物种去重由你决定策略，但要说明）
  - 保留至少20%的物种不出现在训练/验证集中，作为零样本测试集（unseen classes），其余作为seen classes用于微调

## 核心任务（必须全部实现）

### 任务1：零样本真菌分类（CLIP zero-shot）
- 使用openai/clip-vit-base-patch16，冻结所有权重
- 设计多种文本提示模板（至少3种），如：
  - “a photo of a {}”
  - “a macro shot of a {} fungus”
  - “the mushroom {}”
- 在测试集（包含unseen类别）上评估Top-1和Top-5准确率，选出最优模板
- 输出零样本分类的详细评估报告（表格与柱状图）

### 任务2：基于Linear Probe的微调识别
- 在seen类别的训练集上，冻结CLIP图像编码器，仅训练一个线性分类头（Linear Probe）
- 使用交叉熵损失训练，合理设置超参数
- 在测试集（仅seen类别）和验证集上评估，与零样本性能对比
- 分析微调带来的提升，讨论领域适应效果

### 任务3：图像特征库与检索系统
- 使用CLIP图像编码器为所有训练集图像提取特征向量（可统一先提取、存储为.npy或h5文件）
- 构建基于FAISS的向量索引，用于快速相似度检索
- 实现两种检索模式：
  - **以图搜图**：上传一张图像，返回Top-K最相似的训练集图像及其标签，作为参考识别结果
  - **文本搜图**：输入自然语言描述（如“white mushroom with gills”），用CLIP文本编码器获得文本特征，在图像特征库中检索Top-K匹配图像
- 检索结果需可视化展示，并可在Gradio界面中呈现

### 任务4：可解释性分析
- 选择Grad-CAM或其他适合Vision Transformer的可视化方法，生成CLIP图像编码器的注意力热力图
- 至少展示5个测试样本的热力图（零样本方法 vs 微调后方法的对比，若微调后编码器冻结则只做零样本）
- 分析模型关注的特征区域是否合理

### 任务5：交互演示系统
- 使用Gradio构建Web应用，至少包含三个标签页：
  - **零样本识别**：上传图片 → 预测物种 + 概率Top-5 + 热力图
  - **以图搜图**：上传图片 → 显示检索出的相似真菌图片（带物种名）
  - **文本搜图**：输入文字描述 → 显示检索出的真菌图片
- 界面美观、响应迅速、输出结果易读

## 项目结构

这部分你来设计并补充到这里。

## 实验要求与报告内容
- **实验结果分析**：
  - 表格对比零样本、Linear Probe微调在seen/unseen类别上的准确率
  - 消融实验：不同prompt模板对零样本的影响
  - 检索性能评估：mAP@K，或Precision@K
  - 可视化：准确率柱状图、Grad-CAM热力图、检索示例截图
- **实现原理描述**：
  - CLIP图文对齐原理
  - Linear Probe策略与训练细节
  - FAISS索引结构与检索流程
  - 系统架构图
- **工作量体现**：
  - 所有代码模块均自行设计，无直接依赖开源论文实现（仅使用transformers、faiss等基础库）
  - 特别强调“从通用CLIP到真菌领域的迁移适配”是你的核心贡献
  - 描述系统设计的动机与解决实际问题的价值

## 提交内容
- Python源码（src/目录）
- 项目报告（含实验结果分析、原理、参考文献）
- 可选：运行结果演示视频（mp4，可用录屏展示Gradio操作）

## 技术要求
- 深度学习框架：PyTorch
- 关键库：transformers, faiss-cpu, gradio, torchvision, matplotlib, scikit-learn, pandas
- 硬件：单卡GPU（如T4、RTX 3060及以上），训练量很小，无需分布式

## 完成时间
- 从今日起，2周内完成所有代码、报告与演示视频

## 给Claude的特别说明
- 这是一个计算机视觉课程大作业，最终评分看重：选题新颖性、技术深度、系统完整性、实验分析质量、答辩演示效果
- **所有具体技术方案请你自行设计**，包括但不限于：数据预处理策略、prompt设计、Linear Probe训练细节、FAISS索引参数、特征提取批次处理、Gradio布局与交互逻辑
- 代码需注释清晰，便于我理解后写入报告
- 实验数据务必真实运行获得，不要编造
- 有任何需要我确认的决策，请主动询问并给出建议